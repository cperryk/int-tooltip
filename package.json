{
  "name": "int-tooltip",
  "version": "1.1.0",
  "description": "IntTooltip is a simple jQuery-based library for opening a tooltip on a page at a specified element.",
  "main": "intTooltip.js",
  "dependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify-css": "^0.9.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-test": "watchify test/test.js -o test/test.bundle.js -v"
  },
  "author": "Chris Kirk",
  "license": "ISC",
  "browserify": {
    "transform": [
      "browserify-css",
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  },
  "directories": {
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/cperryk/int-tooltip.git"
  },
  "keywords": [
    "tooltip",
    "infobox",
    "popup"
  ],
  "bugs": {
    "url": "https://github.com/cperryk/int-tooltip/issues"
  },
  "homepage": "https://github.com/cperryk/int-tooltip#readme"
}
